{
    "name": "github-i18n",
    "version": "0.2.0",
    "private": true,
    "type": "module",
    "packageManager": "bun@1.4.2",
    "description": "GitHub 界面本地化浏览器扩展（Chrome / Edge，Manifest V3）",
    "scripts": {
        "build": "bun tooling/pipeline/build.ts",
        "watch": "bun tooling/pipeline/build.ts --watch",
        "check": "bun run lint && bun run typecheck && bun run check:dict && bun run check:manifest && bun run check:view && bun run test",
        "lint": "biome check .",
        "format": "biome format --write .",
        "typecheck": "tsc -p tsconfig.json && tsc -p tooling/tsconfig.json",
        "check:dict": "bun tooling/checks/dict.ts",
        "check:manifest": "bun tooling/checks/manifest.ts",
        "check:view": "bun tooling/checks/view.ts",
        "test": "bun test",
        "pack": "bun tooling/pipeline/pack.ts"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.5.14",
        "@types/bun": "^1.4.2",
        "@types/chrome": "^0.3.0",
        "typescript": "^7.0.2"
    }
}
